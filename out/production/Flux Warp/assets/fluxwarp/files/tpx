args = {...}

--teleport, including between dimensions

--find warp core
sides = {'bottom', 'top', 'left', 'right', 'front', 'back'}
for i = 1,#sides do
  if (peripheral.getType(sides[i]) == 'fluxwarpcore') then
    w = peripheral.wrap(sides[i])
    break
  end
end 

w.setDimension(args[1]) --set dimension

--set relative warp coordinates if they are includes
if #args >= 4 then
  w.setWarpVector(args[2], args[3], args[4])
else
  w.setWarpVector(0, 0, 0) --otherwise set to zero
end

w.warp() --activate warp core
